{#def label, value, unit=None, delta=None, delta_dir=None, variant=None, sparkline=None, attrs=None #}

{% macro kpi(label, value, unit=None, delta=None, delta_dir=None, variant=None, sparkline=None, attrs=None) -%}
{%- set _attrs = attrs|attr('as_dict')|default(attrs or {}) -%}
<div class="ok-kpi{% if variant %} ok-kpi--{{ variant }}{% endif %}"
  {%- for key, value_attr in _attrs.items() %} {{ key }}="{{ value_attr }}"{% endfor %}>
  <div class="ok-kpi__label">{{ label }}</div>
  <div class="ok-kpi__value">{{ value }}{% if unit %}<span class="ok-kpi__value-unit">{{ unit }}</span>{% endif %}</div>
  {%- if delta %}
  <div class="ok-kpi__delta{% if delta_dir %} ok-kpi__delta--{{ delta_dir }}{% endif %}">
    {%- if delta_dir == 'up' -%}
      <svg class="ok-kpi__delta-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M7 17L17 7M9 7h8v8"/></svg>
    {%- elif delta_dir == 'down' -%}
      <svg class="ok-kpi__delta-arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M7 7l10 10M17 9v8H9"/></svg>
    {%- endif %}
    {{ delta }}
  </div>
  {%- endif %}
  {%- if sparkline %}{{ sparkline|safe }}{% endif %}
</div>
{%- endmacro %}

{% if label is defined %}{{ kpi(label, value, unit=unit, delta=delta, delta_dir=delta_dir, variant=variant, sparkline=sparkline, attrs=attrs) }}{% endif %}
