{#def title=None, sub=None, variant=None, size=None, attrs=None #}

{% macro panel(title=None, sub=None, variant=None, size=None, attrs=None) -%}
{%- set _attrs = attrs|attr('as_dict')|default(attrs or {}) -%}
<div class="ok-panel{% if variant %} ok-panel--{{ variant }}{% endif %}{% if size %} ok-panel--{{ size }}{% endif %}"
  {%- for k, v in _attrs.items() %} {{ k }}="{{ v }}"{% endfor %}>
  {% if title or sub %}
  <header class="ok-panel__head">
    <div class="ok-panel__head-text">
      {% if title %}<h3 class="ok-panel__title">{{ title }}</h3>{% endif %}
      {% if sub %}<p class="ok-panel__sub">{{ sub }}</p>{% endif %}
    </div>
  </header>
  {% endif %}
  <div class="ok-panel__body">{{ caller() }}</div>
</div>
{%- endmacro %}

{% if title is defined %}
  {% call panel(title=title, sub=sub, variant=variant, size=size, attrs=attrs) %}{{ content }}{% endcall %}
{% endif %}
